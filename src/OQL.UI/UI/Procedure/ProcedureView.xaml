<UserControl x:Class="OneCSharp.OQL.UI.ProcedureView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="clr-namespace:OneCSharp.OQL.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Vertical" Margin="10">

        <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding Keyword}" FontWeight="Bold" VerticalAlignment="Center" Foreground="Blue"
                       MouseEnter="ProcedureHeader_MouseEnter">
                <TextBlock.ContextMenu>
                    <ContextMenu Placement="Bottom">
                        <MenuItem Header="New">
                            <MenuItem Header="Procedure parameter" Click="ContextMenu_AddParameter_Click"/>
                            <Separator/>
                            <MenuItem Header="SELECT statement" Click="ContextMenu_AddSelectStatement_Click"/>
                        </MenuItem>
                    </ContextMenu>
                </TextBlock.ContextMenu>
            </TextBlock>
            <Border BorderThickness="1" Margin="5,0,0,0">
                <Border.BorderBrush>
                    <VisualBrush>
                        <VisualBrush.Visual>
                            <Rectangle StrokeDashArray="2 2" Stroke="Gray" StrokeThickness="1"
                                       Width="{Binding RelativeSource={RelativeSource AncestorType={x:Type Border}}, Path=ActualWidth}"
                                       Height="{Binding RelativeSource={RelativeSource AncestorType={x:Type Border}}, Path=ActualHeight}"/>
                        </VisualBrush.Visual>
                    </VisualBrush>
                </Border.BorderBrush>
                <TextBox Text="{Binding Name}" MinWidth="50" VerticalContentAlignment="Center" BorderThickness="0"/>
            </Border>
        </StackPanel>

        <ItemsControl ItemsSource="{Binding Parameters}"
                          Margin="30,5,0,0"
                          VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <!--<ItemsControl.Resources>
                <DataTemplate DataType="{x:Type ui:ParameterExpressionViewModel}">
                    <ui:ParameterExpressionView Margin="0,5,0,0"/>
                </DataTemplate>
            </ItemsControl.Resources>-->
        </ItemsControl>

        <ItemsControl ItemsSource="{Binding Statements}"
                          Margin="30,20,0,0"
                          VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <!--<ItemsControl.Resources>
                <DataTemplate DataType="{x:Type ui:SelectStatementViewModel}">
                    <ui:SelectStatementView/>
                </DataTemplate>
            </ItemsControl.Resources>-->
        </ItemsControl>

    </StackPanel>
</UserControl>
