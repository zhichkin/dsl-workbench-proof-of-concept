<UserControl x:Class="OneCSharp.AST.UI.SyntaxElementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ast="clr-namespace:OneCSharp.AST.UI"
             mc:Ignorable="d" d:DesignHeight="50" d:DesignWidth="150">

    <UserControl.Resources>
        <Style x:Key="Keyword" TargetType="TextBlock">
            <Style.Setters>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="Blue"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style.Setters>
        </Style>
    </UserControl.Resources>
    
    <ItemsControl x:Name="ROOT_VIEW">
        
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel Orientation="Horizontal"/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        
        <TextBlock Text="SELECT" Style="{StaticResource Keyword}"/>
        <TextBlock Width="5"/>
        <TextBlock Text="FROM" Style="{StaticResource Keyword}"/>
        
    </ItemsControl>
    
</UserControl>
