<UserControl x:Class="OneCSharp.AST.UI.LanguageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="clr-namespace:OneCSharp.AST.UI"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <StackPanel Orientation="Horizontal" Height="20">
        
        <StackPanel.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Add namespace..." Command="{Binding AddNamespaceCommand}">
                    <MenuItem.Icon>
                        <Image Source="{StaticResource Icon_AddNamespace}"/>
                    </MenuItem.Icon>
                </MenuItem>
            </ContextMenu>
        </StackPanel.ContextMenu>
        
        <Image Source="{StaticResource Icon_DomainLanguage}" Height="16" Width="16" VerticalAlignment="Center"/>
        
        <TextBlock Text="{Binding Name}" Margin="2" VerticalAlignment="Center"/>
        
    </StackPanel>

</UserControl>
