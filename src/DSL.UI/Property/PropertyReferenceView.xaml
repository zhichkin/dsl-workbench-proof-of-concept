<UserControl x:Class="OneCSharp.DSL.UI.PropertyReferenceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="clr-namespace:OneCSharp.DSL.UI"
             xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <TextBlock Text="{Binding Name}" Cursor="Hand" ToolTip="Click to select">
        <behaviors:Interaction.Triggers>
            <behaviors:EventTrigger EventName="PreviewMouseLeftButtonUp">
                <behaviors:CallMethodAction TargetObject="{Binding}" MethodName="SelectPropertyReference"/>
            </behaviors:EventTrigger>
        </behaviors:Interaction.Triggers>
    </TextBlock>

</UserControl>
